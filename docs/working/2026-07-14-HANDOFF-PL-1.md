# SESSION HANDOFF — Product Lead — matchmaking scope + link-first duel initiation (2026-07-14)

**Author:** Product Lead session • **For:** the next Product Lead session (fresh context).
**System of record:** git repo `Zuzualvi/yugioh-app`. Latest relevant push: **origin/master @ 603d9ff** (verified).
**Scope of this session:** a product-direction conversation with the CEO that resolved (1) a matchmaking scope defect and (2) a link-first duel-initiation decision. Docs-only changes — no app code touched.

---

## TL;DR — what happened this session
1. **CEO flagged that the CTO agent believed "matchmaking" was in scope** ("deliberately pushed to the next slice"). **Root cause (verified):** the word "matchmaking" was wrongly listed as an infra build item in `docs/working/2026-07-13-CTO-BRIEF.md` line 35 — contradicting HANDOFF / v1-requirements / v1-ux-flows, which all say ranked/matchmaking is out. The CTO's *reasoning* was sound; our brief fed him a wrong word (loose shorthand for the duel-initiation layer).
2. **CEO decided duel initiation goes LINK-FIRST** (Product Lead recommended; CEO confirmed): the shareable invite-to-play link is the primary/only V1 way to start a duel; the in-app online-presence display + directed-challenge system are CUT; pre-duel room + per-move-timer consent are kept. True matchmaking is a **permanent non-goal**, not deferred.
3. **PO + UX produced the rework;** Product Lead consolidated and pushed; CEO confirmed all link semantics.
4. **Delivered to CEO:** a ready-to-paste **kickoff message for a fresh CTO agent** (in chat — see "Open thread" below; not yet in a doc unless the next session wants to persist it).

## Commits pushed this session (both VERIFIED local==remote)
- **af33afb** — matchmaking scope correction (CTO-BRIEF line 35 + HANDOFF Out-of-scope: matchmaking = permanent non-goal, distinguished from the in-V1 duel-initiation layer).
- **603d9ff** — full link-first rework (consolidated, docs-only):
  - `2026-07-13-v1-requirements.md` — §4 REQ-LOBBY 01–06 rewritten in place (IDs preserved); R9 collapsed to a single atomic seat-claim invariant; AC-02 rewritten; AC-22 cross-ref fixed (LOBBY-02→LOBBY-01); §17 Q7 (timer default) resolved = 24 h; count table LOBBY 3/3/0/6→4/1/0/5, totals 91/19/3/113→92/17/3/112.
  - `2026-07-13-HANDOFF.md` — new **decision 6** (link-first) + rewritten Lobby/screens/timer-UX summaries.
  - `2026-07-13-CTO-BRIEF.md` — line 35 now points to the link-first delta docs.
  - `2026-07-13-v1-ux-flows.md` — top SUPERSEDED banner + §1 flow-map rewrite + §3 (Home) and §4 (Challenge) superseded markers (old wireframes retained as historical context, clearly marked dead for initiation).
  - **NEW** `2026-07-14-link-first-lobby-change.md` (PO delta) + `2026-07-14-link-first-initiation-flow.md` (UX delta).

## CEO-CONFIRMED decisions (pinned)
- Duel initiation = **LINK-FIRST**; matchmaking = **permanent non-goal** (never, not V2).
- Link semantics: **single-use** links · unopened-link expiry **7 days** (fixed, not a picker) · default per-move timer **24 h** · **open to any authenticated member**, first opener claims the seat (atomic single-claim) · creator **can revoke** an unopened link · **one outstanding outgoing invite per member** · link must **never leak the creator's decklist** · **non-members denied** without consuming the link.
- Decision record: `/mnt/memory/yugioh-app-team-memory/decisions/2026-07-14-link-first-duel-initiation.md` (status: RESOLVED + SHIPPED).

## Resolved in Product-Lead lane (not CEO decisions)
- "Non-member opening a link" = anyone without an account (invite-only, no self-signup) → members-only wall; there is **no "authenticated-non-member" class** to build.
- The account-onboarding invite link and the invite-to-**play** link MUST stay distinct in copy + code (UX catch).

---

## Open thread / what the next session should pick up
1. **Awaiting CEO reply to one question:** whether to tweak the CTO kickoff message, and whether the Product Lead should paste the brief into a new CTO agent's thread once the CEO spins one up. (The kickoff message was delivered in chat; if the CEO wants it persisted, save it as a doc.)
2. **UNVERIFIED — needs CTO reconciliation, not confirmed this session:** the dueling slice already landed a **create/join-link** flow (web slice 30, server slice 20 — per `2026-07-14-HANDOFF-STATE.md`). Whether that existing code actually satisfies the *formalized* link-first semantics (single-use, atomic seat-claim, 7-day expiry, revoke, one-outstanding-invite, no-decklist-leak, non-member denial) has **not** been verified — it is a punch-list task flagged for the CTO. Do not assume it's done.
3. **Non-blocking, for CTO/eng:** confirm the identity/auth model can distinguish "group member" vs "no account" for the members-only link wall; decide whether to add an "I don't have a login" affordance on the join screen.
4. **Not this session's concern but noted:** the CTO reports **CI is RED** on recent master and the custom WASM engine artifact isn't built yet (empirical rule tests skipped). See `2026-07-14-HANDOFF-STATE.md`. That's the CTO's lane.

## Where to read (authoritative, in order)
- Product context/decisions: `docs/working/2026-07-13-HANDOFF.md` (trail starts at `2026-07-13-INDEX.md`), `2026-07-13-CTO-BRIEF.md`.
- Requirements: `docs/working/2026-07-13-v1-requirements.md` (§4 = link-first lobby).
- Link-first deltas: `docs/working/2026-07-14-link-first-lobby-change.md` + `-initiation-flow.md`.
- Team memory: `project-context.md` / `yugioh-edison-app-project.md` (narrative) + `decisions/2026-07-14-link-first-duel-initiation.md`.
- Repo build rules: `AGENTS.md`. Shared-tree git protocol (detached HEAD is normal): push `git push origin HEAD:master`, pull `git pull --rebase --autostash origin master`, `git add` only owned paths.
