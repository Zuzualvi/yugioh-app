# Session Handoff #1 — Edison Deck Builder go-live

**Date:** 2026-07-13
**From:** CTO session (context-size checkpoint)
**Repo state at handoff:** `master` @ `1ddc1fb`, working tree clean.
**Scope of this session:** took the Edison deck builder LIVE via the `provision_hosting` tool (Vercel frontend + Fly backend split). No product/feature work beyond an invite-generation UI that was missing.

---

## Current state: LIVE (verified this session by direct curl)

| Tier | URL | Host | Evidence |
|------|-----|------|----------|
| Frontend | https://app.zuhayr.io | Vercel (git-integration auto-deploy) | `200` HTML from Vercel; bundle contains `https://api.zuhayr.io` + `https://api.zuhayr.io/images`, zero `ygoprodeck`; SPA deep-links (`/decks`, `/builder/:id`) → `200` via `vercel.json` rewrite |
| Backend | https://api.zuhayr.io | Fly.io app `yugioh-app`, region `ord` | `/healthz` → `{"status":"ok","cards":3681}`, valid TLS; CORS preflight from `app.zuhayr.io` → `204` exact-origin+credentials; `/api/cards` no-auth → `401` |
| Card images | https://api.zuhayr.io/images/*.jpg | Fly volume `data` | sampled ids all `200 image/jpeg` (~130–165 KB) — full seed ran |

Admin bootstrap: username **`zuzu`** (password = CEO-set GitHub secret `BOOTSTRAP_ADMIN_PASSWORD`). Invite flow: admin "Invite a friend" button on HomeScreen → link `app.zuhayr.io/login?invite=<code>` (single-use, 7-day). No email.

---

## What was built/changed this session (all pushed to `master`)

1. **Deploy split rework** (Spec 21, `docs/working/2026-07-13-spec-21-deploy-split-rework.md`) — the Vercel+Fly split was decided earlier but never coded. Landed:
   - Backend CORS allowlist middleware (`packages/server/src/middleware/cors.ts`) + session cookie `SameSite=Lax`.
   - Frontend env-driven API/image base (`packages/web/src/api/client.ts`, `.../utils/cardImageUrl.ts`).
   - Backend made API-only (`prod-server.ts` no longer serves SPA); Dockerfile slimmed.
2. **Admin "Invite a friend" UI** (`packages/web/src/screens/HomeScreen.tsx`, `.../api/admin.ts`) — invite generation had no UI; backend endpoint already existed.
3. **Provisioning** via `provision_hosting`: Fly app `yugioh-app` + volume `data`, Vercel project `yugioh-app`, DNS, repo secrets.
4. **CI wiring** (I own workflow files — harness git token has workflow scope):
   - `.github/workflows/deploy.yml` — merge=deploy, **backend only**: `verify` → `fly deploy` → allocate IPs → `certs add` → seed images. `paths-ignore: docs/**, **/*.md`.
   - `.github/workflows/ci.yml` — `verify` on PRs.
   - Image seeding: `deploy/seed-images.mjs` copied into runtime image; run post-deploy via `flyctl ssh console -C`.
5. **Reports:** `docs/working/2026-07-13-GO-LIVE-report.md` (+ `/mnt/session/outputs/REPORT.md`).

Clean-clone `npm run verify` = **179 tests green** (typecheck + lint + arch:check 0 violations + tests).

---

## Deploy model (how it works now)

- **Frontend** = Vercel **Git integration** auto-deploys on push (uses Vercel project env vars `VITE_API_BASE_URL=https://api.zuhayr.io`, `VITE_IMAGE_BASE_URL=https://api.zuhayr.io/images`, which the tool set correctly). CEO **confirmed** the Deployments tab shows git-triggered builds. There is deliberately **no** CI frontend job (a second CLI deployer fought with git-integration and hung).
- **Backend** = GitHub Actions `deploy.yml` on push to `master` (non-docs). Idempotent seed step.
- Repo secrets: `FLY_API_TOKEN`, `VERCEL_TOKEN`, `VERCEL_ORG_ID`, `VERCEL_PROJECT_ID` (VERCEL_* now unused by CI — harmless), `BOOTSTRAP_ADMIN_USERNAME`, `BOOTSTRAP_ADMIN_PASSWORD`.

---

## Environment constraints for the NEXT session (important)

- **No CI/dashboard visibility from the CTO sandbox.** The git credential is an Anthropic-managed **git-only** token (prefix `ANTH`); it returns `404` on ALL GitHub REST API calls (repos, actions, checks, secrets). Granting the CEO's PAT more scope does NOT help. **Monitor deploys by curling the live endpoints** (`api.zuhayr.io/healthz`, `app.zuhayr.io`) — that is the working monitoring path.
- No `gh`/`flyctl`/`vercel` CLI and **no Docker daemon** in the sandbox — cannot build/deploy locally; rely on CI + live-endpoint verification.
- Team memory (rich history): `/mnt/memory/yugioh-app-team-memory/yugioh-edison-app-project.md` (project log) + `/decisions/2026-07-13-deploy-execution-provision-hosting.md`.

---

## Open items

- **[BLOCKED ON CEO — awaited]** CEO is working with the PRODUCT team on a separate thread to adjust requirements after real usage, then will deliver a **CHANGE READOUT for the CTO to review** before any next build. **Do not start new build work until that readout arrives.** Review it for architecture fit / cost / scope before speccing.
- **[NEXT BIG BUILD, not started]** P2-DUELING: real-time + async duels, ocgcore WASM engine over WebSockets, per-duel forfeit timer, response-log replay persistence. Redeploys the SAME Fly backend. All de-risking spikes (A/A2/B/C/D/E) already green — see memory.

## Minor / unverified

- **Region `ord` (Chicago), not `iad`** — the provisioner's default won over the region hint. Fine for a US-East group; `fly.toml` aligned to `ord`. Migratable later; *not verified* whether CEO wants a move (they were fine with it).
- The `data` volume region is inferred to be `ord` (the running machine is in `ord` and mounts it); *not directly verified* via `fly volumes list` (no CLI access).
- Everything above marked with concrete evidence was cur/test-verified this session; anything not so marked is inference.
