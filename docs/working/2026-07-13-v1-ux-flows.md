# V1 Screen Flows & Wireframes — Yu-Gi-Oh (Edison) Duel App

**Author:** UX/UI (subagent) · **Date:** 2026-07-13 · **Status:** wireframe-level build handoff (pre-visual-design)
**Audience:** CTO / engineering + CEO. This is the screen-and-flow spec the build hangs off.
**Builds on:** `/workspace/product/research/ux-landscape.md` (competitive teardown + principles + screen inventory). Read that first; this document turns its inventory into per-screen wireframes and the flows between them.

---

## What changed from the prior brief (LOCKED DECISIONS, CEO 2026-07-13)

V1 **enforces** rules but does **NOT explain illegality**. Concretely, relative to the prior brief:

**REMOVED from V1 (moved to V2):**
- The "why is this greyed out?" plain-language reason tooltips (prior brief's B3 "ⓘ why?" chip).
- Deep-links from disabled actions into the Rules & Rulings page.
- The shared `{engine reason → human string → rules anchor}` reason-mapping layer.

**KEPT in V1 (do not design these out — they are enforcement / surfacing legal moves, not explaining illegality):**
- **(a)** Marking cards/actions that currently *have* a legal action available (the "actionable" glow/dot).
- **(b) Priority / quick-effect RESPONSE WINDOWS** — when the opponent acts and the player has a legal response (quick effect / trap / Spell Speed 2+), the UI pauses and offers it; when they have none, no prompt. Engine-driven.
- **(c)** Chain-stack visualization + step-by-step resolution playback.
- **(d)** Deck-builder legality **badges** (Forbidden / Limited / Semi) and blocking illegal decks.

Disabled/illegal actions may still be shown **dimmed** (as a *state*) — they simply do not get a tappable text explanation in V1. The moment a "no" would want a sentence attached, that sentence is a V2 feature. Design leaves a clean seam for it (see §16) but ships nothing that renders it.

**Architecture-agnostic:** server-authoritative vs. WASM, and image hosting, are the CTO's call. These screens assume only that (1) the engine surfaces the set of currently-legal decisions/actions for the active player, and (2) clean, legible card images exist at multiple sizes.

---

## How to read this

- **§0 Global conventions** — the recurring primitives (card states, the actionable marker, tap targets, responsive tiers, color+icon pairing). Defined once so per-screen sections stay short.
- **§1 Flow map** — the whole app as a transition graph, including the challenge/accept loop and the duel start/end loop. One-line summary at top.
- **§2–§14** — one section per screen: **Purpose · Key elements · Primary interactions · Desktop wireframe · Mobile (portrait) wireframe where it differs · States · Notes**.
- **§15 Mobile 2-player board** — the central V1 UX risk, treated head-on (carried forward + refined).
- **§16 Accessibility & readability** — non-negotiable requirements, consolidated.
- **§17 Handoff notes / open questions for eng.**
- **§18 Explicitly NOT in V1.**

ASCII wireframes are structural, not pixel-accurate. `[ ]` = tappable control/zone, `▸`/`▾` = disclosure, `•` = the actionable marker, `▓/░` = filled/empty meter.

---

## §0. Global conventions (defined once, reused everywhere)

**Card visual states** (one grammar across board, hand, builder, inspector):

| State | Meaning | Rendering (color + non-color cue) |
|---|---|---|
| **Actionable** | Card has ≥1 legal action *right now* | Normal art **+ glow ring + corner dot `•`** |
| **Idle** | On field/in hand, no legal action this instant | Normal art, no marker |
| **Dimmed** | Face-down/unavailable state, or invalid target during a selection step | ~55% opacity **+ no highlight** (valid targets *pulse*; invalid ones simply don't) |
| **Selected** | Currently picked in a multi-step action | Solid accent border **+ checkmark** |
| **Face-down** | Set / hidden | Card-back art (never the face; hidden info never rendered client-side) |

No state is conveyed by color alone — each pairs color with a shape/icon/label (colorblind-safe; ~8% of men). In V1, a Dimmed card is **not tappable-for-reason**; it is still tappable-to-**inspect** (reading a card is always allowed — see §9).

**The actionable marker** is the literal implementation of "surface legal actions; don't make the player hunt." If a card or zone can do something this instant, it is marked. If nothing on your side is marked and it's your turn, the only legal move is to advance the phase — and the phase rail says so.

**Ownership cue:** *your* elements use the primary accent + a solid underline; *opponent* elements use a secondary hue + a dashed/hatched underline. Never rely on top-vs-bottom alone (landscape/tablet can reflow).

**Tap targets ≥ 44 px; body text ≥ 16 px on mobile;** both honor the OS/browser text-size setting. Every destructive/committing action is confirmable or cancelable; nothing commits on a single accidental touch (see inspect-vs-act separation, §15).

**Responsive tiers — one layout system, three densities:**
- **Phone-portrait** (~360–430 px): your-field-first, opponent as a status strip (§15).
- **Tablet / small-laptop** (~768–1024 px): both fields, condensed.
- **Desktop** (≥1024 px): full board (§6).

Same components, same interaction grammar, reflowed — so a friend on a couch phone and a friend at a desk have the same competence.

**Neutral event language (V1 constraint).** Anywhere the app narrates what happened (priority prompts, chain captions, duel log), it states the **event**, never a *judgment* about legality: "Alex activated Bottomless Trap Hole" ✓; "You can't activate that because…" ✗ (that's V2). This keeps the copy layer honest to the no-explanation decision.

---

## §1. FLOW MAP

**One line:**
`Login → Home/Lobby ⇄ {Deck Builder ⇄ My Decks · Rules & Rulings · Settings} ; Home → Challenge → (opponent Accepts) → Pre-Duel Room → Duel Field ⇄ {Card Inspector · Priority/Chain prompts} → Duel Summary/Replay → (Rematch → Pre-Duel Room | Home)`

**Transition graph:**

```
                         ┌────────────────────────────────────────────┐
                         │                                            │
   ┌────────┐   ok   ┌───▼─────────┐                                  │
   │ LOGIN  ├───────►│ HOME/LOBBY  │◄───────────────┐                 │
   └────────┘        └─┬─┬─┬─┬─┬───┘                │                 │
   (invite link/           │ │ │ │ │                    │  "Back to lobby"    │
    passcode)              │ │ │ │ └────────────► SETTINGS / PROFILE  │
                          │ │ │ │                                     │
                          │ │ │ └──────────────► RULES & RULINGS ─────┤ (standalone;
                          │ │ │                                        │  reachable
                          │ │ └──► MY DECKS ⇄ DECK BUILDER ───────────┤  from anywhere)
                          │ │        (new/edit/dup/import/export)      │
                          │ │                                         │
                          │ └──────────────────────────────┐         │
                          │        CHALLENGE FLOW           │         │
                          │  pick friend│share link ──► pick deck ──► send
                          │                                 │         │
                          │        (incoming challenge banner on Home)│
                          │        Accept ─► pick deck ─────┘         │
                          ▼                                            │
                   ┌─────────────┐   both READY + first-turn set      │
                   │ PRE-DUEL    ├──────────────► START ──┐           │
                   │   ROOM      │◄──── Rematch ───────┐   │           │
                   └─────────────┘                     │   ▼           │
                          │ (either leaves)            │  ┌──────────────┐
                          └──────────► HOME            │  │  DUEL FIELD  │
                                                       │  │  (core loop) │
                                                       │  └──┬───────┬───┘
                            inspect (tap art) ◄────────┼─────┘       │
                          ┌──────────────┐             │             │ engine emits
                          │ CARD INSPECTOR│────────────┘             │ decision:
                          └──────────────┘  (overlay; returns)       │ priority/target/
                                                                     │ chain
                                                       ┌─────────────▼─────────────┐
                                                       │ PRIORITY / RESPONSE PROMPT │
                                                       │  + CHAIN STACK + RESOLVE   │
                                                       │  (overlay on field)        │
                                                       └─────────────┬─────────────┘
                                                                     │ resolves → back to field
                                    duel ends (LP 0 / deck-out /     │
                                    surrender / DC)                  ▼
                                                       ┌───────────────────────────┐
                                                       │  DUEL SUMMARY / REPLAY     │
                                                       │  win/loss · LP graph · log │
                                                       └──────┬─────────────┬───────┘
                                                       Rematch│             │ Done
                                                              ▼             ▼
                                                        PRE-DUEL ROOM     HOME
```

**Two loops called out explicitly (per task):**
- **Challenge/accept loop:** `Home → Challenge (pick friend/share link → pick deck → send)`; the challenged friend sees an **incoming-challenge banner on their Home**, taps `Accept → pick deck`; both land in the **Pre-Duel Room**. Decline or timeout returns the challenger to Home with a status note.
- **Duel start/end loop:** `Pre-Duel Room → (both Ready + first-turn decided) → Start → Duel Field → (duel ends) → Duel Summary/Replay → Rematch (→ Pre-Duel Room, decks pre-filled) OR Done (→ Home)`.

Rules & Rulings and Settings are reachable from Home and (as a slide-in) from within the Duel Field, so a player never has to abandon a duel to check a rule or bump text size.

---

## §2. Login

**Purpose.** Get the ~6 known friends in with zero ceremony; keep everyone else out. This is a closed group, not a product with sign-ups — no discovery, no social graph, no password-reset maze.

**Key elements.** App wordmark; one primary path (**invite link** the founder sends → sets display name + passcode on first use) and one returning path (**display name/email + passcode**); a single "trouble getting in? ask the group" line (no self-serve reset in V1 — the founder re-issues links). No "create account" button visible to the public.

**Primary interactions.** First visit via invite link → set display name + passcode → land on Home. Returning → enter credentials → Home. Invalid → inline error, no enumeration ("that didn't match" — never "no such user").

**Desktop & mobile — same single-column card, centered.** (No meaningful layout divergence; it's one form.)

```
┌───────────────────────────────┐
│                               │
│        ⟡  EDISON DUEL         │
│      a private duel club       │
│                               │
│   Display name [___________]  │
│   Passcode     [___________]  │
│                               │
│        [   Enter  ▸   ]        │
│                               │
│   First time? Open your        │
│   invite link to set up.       │
└───────────────────────────────┘
```

**States.** Default · invalid credentials (inline) · invite-link setup (adds "confirm passcode") · already-signed-in (skip to Home).

**Notes.** Minimal by design — the two things people come to do (duel, tweak a deck) are one tap past this screen, so this screen must not editorialize. Session persists so friends aren't re-authing every visit.

---

## §3. Home / Lobby

**Purpose.** The group's front door. Answers "who's around and what do I want to do?" in one glance. Optimized for a tiny known group, so it feels like a group chat that starts duels — **not** a matchmaking queue.

**Key elements.**
- Three primary actions: **Duel a friend · Build a deck · Rules & rulings.**
- **Who's online** — the group roster with presence dots (online / in a duel / offline). Small; it's ~6 people.
- **Pending challenges** — incoming ("Alex challenges you") with Accept/Decline; outgoing ("waiting for Sam…") with Cancel.
- Header: your display name/avatar → menu to **Settings** and sign-out.

**Primary interactions.** Tap **Duel a friend** → Challenge flow (§4). Tap a roster name who's online → shortcut into Challenge flow pre-targeted at them. **Accept** an incoming challenge → deck pick → Pre-Duel Room. **Build a deck** → Deck Builder / My Decks. Incoming challenge also surfaces as a toast if you're mid-navigation elsewhere.

**Desktop:**

```
┌───────────────────────────────────────────────────────────────┐
│  ⟡ Edison Duel                                   ● You  ▾        │
├───────────────────────────────────────────────┬───────────────┤
│                                                │  GROUP         │
│   ┌───────────┐ ┌───────────┐ ┌────────────┐  │  ● Alex        │
│   │  ⚔        │ │  🂡        │ │  📖         │  │  ● Sam (in duel)│
│   │ Duel a    │ │ Build a   │ │ Rules &    │  │  ○ Priya       │
│   │ friend    │ │ deck      │ │ rulings    │  │  ○ Mo          │
│   └───────────┘ └───────────┘ └────────────┘  │                │
│                                                │  CHALLENGES    │
│   Recent: last duel vs Sam — you won  [Replay] │  ▸ Alex → you  │
│                                                │    [Accept][✕] │
│                                                │  ▸ you → Priya │
│                                                │    waiting…[✕] │
└───────────────────────────────────────────────┴───────────────┘
```

**Mobile (portrait):** roster + challenges collapse under the three actions; incoming challenges pin to the top as a banner (highest priority — it's a live invite).

```
┌───────────────────────┐
│ ⟡ Edison Duel     ☰   │
├───────────────────────┤
│ ▸ Alex challenges you  │  ← pinned banner if pending
│   [ Accept ] [ ✕ ]     │
├───────────────────────┤
│  ┌─────────────────┐   │
│  │ ⚔ Duel a friend │   │
│  └─────────────────┘   │
│  ┌─────────────────┐   │
│  │ 🂡 Build a deck  │   │
│  └─────────────────┘   │
│  ┌─────────────────┐   │
│  │ 📖 Rules&rulings│   │
│  └─────────────────┘   │
├───────────────────────┤
│ GROUP  ● Alex ● Sam▪   │  ← ▪ = in duel
│        ○ Priya ○ Mo    │
├───────────────────────┤
│ Last: vs Sam — won ▸   │
└───────────────────────┘
```

**States.** No one online (roster greyed, "no one's around — send an invite link") · incoming challenge · outgoing challenge (waiting/declined/expired) · returning-from-duel (shows Replay of last game).

**Notes.** No ranked ladder, no unread badges beyond challenges — this screen must not manufacture engagement for six friends. The whole point is *low ceremony*.

---

## §4. Invite / Challenge-a-friend flow

**Purpose.** "Let's duel right now" in seconds, with no external tool (beats EDOPro's Discord dependency). Two entry modes: pick a friend who's here, or share a link to pull in someone who isn't.

**Key elements.** Step 1 — **Who:** roster with presence, or **[Copy invite link]** (drop into the group chat; link deep-links the recipient to Accept). Step 2 — **Your deck:** pick from My Decks (shows legality chip; illegal decks can't be sent — see §11/§10). **[Send challenge]**.

**Primary interactions.** Pick friend → pick deck → Send → outgoing-pending state on Home + a prompt fires on their side. If they Accept → both to Pre-Duel Room. Share-link path: generate link → recipient opens → (login if needed) → Accept → pick deck → Pre-Duel Room. Cancel any time before acceptance.

**Desktop (two-step, single modal over Home):**

```
┌──────────────── CHALLENGE A FRIEND ─────────────────┐
│  Step 1 · Who?                                       │
│  ● Alex   ● Sam(in duel — busy)   ○ Priya   ○ Mo     │
│  ────────────────────  or  ────────────────────      │
│  [ 🔗 Copy invite link ]  (share in your group chat) │
│                                                      │
│  Step 2 · Your deck                                  │
│  ( ) Quickdraw Plant   ✓ Edison-legal                │
│  (•) Blackwings        ✓ Edison-legal                │
│  ( ) Test brew         ⚠ 2 issues — can't send       │
│                                                      │
│                 [ Cancel ]     [ Send challenge ▸ ]  │
└──────────────────────────────────────────────────────┘
```

**Mobile (portrait):** same two steps stacked as a full-screen sheet; "Who" first, then a deck picker; "Send" pinned to the bottom. Offline friends are shown greyed with "invite link" as the only way to reach them.

**States.** Friend selected · link copied (toast) · deck selected · deck **illegal → Send disabled** with the deck's issue count shown (this is enforcement/legality surfacing — allowed in V1; it does not narrate *why* a specific card is illegal beyond the badge) · challenge sent (→ outgoing pending) · recipient busy/offline.

**Incoming side (what the challenged friend sees):** the Home banner/toast in §3 → **Accept** opens the same Step-2 deck picker for them → Pre-Duel Room. **Decline** returns a status to the challenger.

**Notes.** No lobby chat here; trash talk lives in the Pre-Duel Room and (optionally) the duel. Keep this flow to *who + which deck*, nothing else.

---

## §5. Pre-Duel Room

**Purpose.** The social "shuffle up" moment: confirm both players, both decks, and who goes first, then Start. Preserves the ritual without being slow.

**Key elements.** Two player cards (name, avatar, chosen deck name + legality chip, **Ready** toggle); **first-turn decision** widget (coin flip / dice — winner *chooses* to go first or second, per real YGO); **[Start duel]** (enabled only when both Ready and first turn resolved); lightweight **text chat**; **[Change deck]** and **[Leave]**.

**Primary interactions.** Each player taps **Ready** (can change deck until Ready). First-turn: one animated coin/dice roll visible to both → winner picks first/second. When both Ready + choice made → **Start** enables for both; either can start (or auto-start on a short shared confirmation). Leaving returns both to Home with a note.

**Desktop:**

```
┌──────────────────── PRE-DUEL ROOM ─────────────────────┐
│                                                        │
│   YOU                        │        ALEX             │
│   [avatar]                   │   [avatar]              │
│   Deck: Blackwings ✓         │   Deck: Quickdraw ✓     │
│   [ ✓ Ready ]                │   [ ⏳ not ready ]       │
│                                                        │
│   ── FIRST TURN ─────────────────────────────────────  │
│   🪙  Coin toss: ALEX won → Alex chooses to go FIRST    │
│                                                        │
│   [ Change deck ]                     [ Leave ]         │
│                                                        │
│                 [   Start duel  ▸   ]  (waiting on Alex)│
│                                                        │
│   ── CHAT ───────────────────────────────────────────  │
│   Alex: gl hf                                          │
│   [ type…                                        ] [→] │
└─────────────────────────────────────────────────────────┘
```

**Mobile (portrait):** the two player cards stack vertically (you on top), first-turn widget between them, chat collapses to a tab/peek sheet, **Start** pinned bottom.

**States.** Waiting-on-opponent-ready · both-ready-first-turn-pending · ready-to-start · opponent-left (→ back to Home) · rematch-entry (decks pre-filled from last duel; first turn re-rolled, or loser chooses — TBD with group).

**Notes.** First-turn = coin/dice per real rules (not a silent auto-assign) because the group plays in person and expects the ritual. Note (per §16/domain): in Edison (pre-MR3) the player going **first draws on turn 1** — the duel start reflects this; no special UI, just correct engine config.

---

## §6. Duel Field — DESKTOP (full board)

**Purpose.** The core screen. At all times it must answer three questions at a glance: **Whose turn/phase? What's the score? What is the game waiting on *me* for?** Everything else is subordinate to those three.

**Edison board = simpler than modern** (Master Rule 1 era): per player = 5 Monster Zones, 5 Spell/Trap Zones, 1 Field Spell Zone, Deck, **Extra Deck (Fusion + Synchro only)**, Graveyard, Banished. No Extra Monster Zones, no Pendulum scales, no Link arrows. We exploit the extra room for breathing space and legibility, not clutter.

**Key elements.** Opponent status bar (id, LP bar+number, face-down hand count); opponent field mirrored across a center **turn/phase ribbon**; your field nearest you; your **hand tray** (fanned, readable, actionable marker on playable cards); persistent **phase rail** + **End Turn**; a **duel log** rail (collapsible) that neutrally lists events; utility (Settings slide-in, Surrender, Rules slide-in).

**Primary interactions.** Click a card → its legal actions appear (context menu on desktop; same action-sheet grammar as mobile so the model is identical). Click art / hover-pin → **Card Inspector** (§9). Advance phase via the rail or End Turn. Valid targets pulse during selection steps; invalid ones stay un-highlighted. Drag is offered as an *optional* accelerator on desktop but never required.

```
┌──────────────────────────────────────────────────────────────────────────┐
│  OPP ● Alex    LP ▓▓▓▓▓▓▓░░ 5400          Hand 🂠🂠🂠🂠 (4)      ⚑ Surrender │
├──────────────────────────────────────────────────────────────────────────┤
│                                                             ┌── LOG ─────┐ │
│  [GY][Ban]  [ M ][ M ][ M ][ M ][ M ]        [Extra][Deck]  │ T3 Alex NS │ │
│  [Field]    [S/T][S/T][S/T][S/T][S/T]                       │  Cyber D…  │ │
│ ───────────────────  ⚔  ALEX'S BATTLE PHASE  ─────────────  │ T3 you Set│ │
│  [Deck][Extra]  [S/T][S/T][S/T][S/T][S/T]         [Field]   │  Mirror F.│ │
│  [GY][Ban]      [ M ][ M ][ M ][ M ][ M ]                   │ …          │ │
│                                                             └────────────┘ │
│  YOU ● you     LP ▓▓▓▓▓▓▓▓▓ 8000                                           │
├──────────────────────────────────────────────────────────────────────────┤
│  YOUR HAND   [card] [card] [card•] [card] [card]     ( • = playable now )  │
├──────────────────────────────────────────────────────────────────────────┤
│  PHASE:  DP → SP → [MP1] → BP → MP2 → EP            [ End Turn ▸ ]          │
└──────────────────────────────────────────────────────────────────────────┘
```

**Design decisions → outcomes.**
- **Your half nearest you, opponent mirrored across the center ribbon** → matches the tabletop mental model; zero re-orientation cost.
- **Persistent phase rail**, current phase highlighted, reachable phases enabled → the most common "what now?" (which phase, how to move on) is answered permanently, and turn structure is learned passively. When it's your turn and nothing on your side is marked actionable, the only lit control is the phase rail — the UI is telling you "your move is to advance."
- **Center ribbon states whose turn + phase in words** ("Alex's Battle Phase" / "Your Main Phase 1").
- **LP as bar + number with animated deltas** ("−1800" floats, bar drains) → the scoreboard/drama is unmissable and damage math is auditable.
- **Actionable marker** on any card with ≥1 legal action this instant → surfaces options; strategy over pixel-hunting.
- **Opponent hand = face-down count only** (hidden info is never sent to the client; server-authoritative per engine research).
- **Duel log rail** neutrally lists events (turn #, actor, action) — the reviewable spine that also feeds the Summary/Replay (§13). Neutral language only (no "why").

**States.** Your turn / opponent's turn (your controls dim to *read-only* except responses) · selection-in-progress (targets pulse) · **awaiting your response** (§7 overlay) · animation/resolution playback · duel over (→ §13). Reduced-motion mode swaps animated LP/chain feedback for instant, still-legible updates.

---

## §7. Priority / Quick-effect Response Prompt + Chain Stack + Resolution Playback

**KEPT in V1** (per boundary: this is enforcement / surfacing legal responses, *not* explaining illegality). This is the highest-stakes moment for clarity, and where the enforcing engine earns its keep. The UI **slows down** here (the opposite of Master Duel's pace) without feeling sluggish.

**Engine-driven, both directions.** The engine only prompts a player when they *have* a legal decision to make:
- **Responding to the opponent:** opponent acts → if you hold a legal quick effect / trap / Spell-Speed-2+ response, the prompt appears; if you hold none, **no prompt** (engine auto-passes) — you're never asked a question you can't answer.
- **Your own priority (Edison-specific — Ignition Effect Priority is IN EFFECT this format):** after you Summon, if you have a legal Ignition Effect to activate before passing, the *same* prompt offers "activate an effect or pass priority?" This must be configured in the engine ruleset (see §17) — the UI presents it identically to a response window.

**Purpose.** Never let a player accidentally pass on a response they wanted (a classic frustration); make the *order* things resolve visible, because chain order is the single most-misunderstood rule and the thing judges correct people on in person.

**Key elements.**
1. **Priority prompt** — an unmissable but *non-blocking-of-context* bar: the board stays visible behind it. States the triggering **event** neutrally + offers **[Respond ▸]** / **[Pass]**.
2. **Response picker** — if Respond: the list of *your legal responses only* (cards + effects that can be activated now, each with the actionable marker). No greyed "why not" lines in V1.
3. **Chain stack** — vertical, Link 1 at the **bottom** (resolves last) → top resolves first; each link = mini card + owner color/hatch.
4. **Resolution playback** — resolves **top-down**, brief highlight per link + a plain **event** caption; LP/board deltas animate (or snap, in reduced-motion).

**Priority prompt (overlay on the live field):**

```
        ┌──────────────────────────────────────────────┐
        │  Alex activated  Bottomless Trap Hole.         │  ← neutral event
        │  You may respond.                              │
        │            [ Respond ▸ ]        [ Pass ]       │
        └──────────────────────────────────────────────┘
```

**Response picker (only legal responses listed):**

```
        ┌──────────────── RESPOND WITH ────────────────┐
        │  •  Solemn Judgment  (Trap, from S/T zone)     │
        │  •  Starlight Road    (Trap, from S/T zone)    │
        │                                    [ ✕ Back ]  │
        └────────────────────────────────────────────────┘
```

**Chain stack + resolution (right-docked on desktop, sheet on mobile):**

```
        ┌── CHAIN ─────────────────────┐        ── RESOLVING ──
        │ CL3  Solemn Judgment  (you) ▲ │  →  ▶ Link 3: Solemn Judgment
        │ CL2  [add response?]         │           (you) negates & destroys…
        │ CL1  Bottomless T.H.  (Alex) │      then Link 2 … then Link 1
        └───────────────────────────────┘
        [ Add to chain… ]   [ Let it resolve ▸ ]
```

**Primary interactions.** Respond → pick from legal responses → it stacks as the next chain link → prompt passes back until both pass → **resolve top-down** with per-link captions → return to field. Pass → engine resolves. On mobile the prompt is a bottom sheet with thumb-reachable **[Respond]/[Pass]**; the chain stack is a peekable panel.

**States.** Awaiting-your-response · building-chain (your turn to add / opponent's) · resolving (step highlight) · empty-response (never shown — engine auto-passes). No countdown/timer in V1 (friends), but the prompt bar reserves space for one later.

**Notes.** Every prompt is phrased as *the question the engine is asking* ("You may respond," "Select a target"), stating events neutrally — never raw engine state, and never a why-not sentence. This is the seam the V2 chatbot slots into with no layout change.

---

## §8. (reserved) — Targeting & selection

Not a separate screen; it's an in-field mode of §6/§7. Called out so eng doesn't miss it: during any "select target(s)/zone" decision the engine requests, valid destinations **pulse** and invalid ones stay flat (Dimmed grammar from §0). A persistent mini-prompt states the ask ("Select 1 monster to target · 0/1") with **[Confirm]/[Cancel]** and a running count for multi-select. Cancel unwinds to before the action started (nothing commits until Confirm) — critical for the tap-first, mistake-forgiving model.

---

## §9. Card Inspector (reused everywhere)

**Purpose.** Reading the card is sacred. Dense Yu-Gi-Oh text is the game's core information; it must never be trapped behind a hover or a squint, and reading must **never** risk triggering a play. Reused by the field, the builder, the rules page, and the summary/replay.

**Key elements.** Large card art; **full name, type line, Attribute, Level/Rank (Level only in Edison), ATK/DEF**; **complete effect text at readable size**; set/passcode; for Edison, static **rulings/notes** where we have verified content (read-only reference text — *not* a live "why did this happen" — that's V2). Nav: **[✕ Close]**, and where launched from a list, **‹ ›** to page between cards.

**Primary interactions.** Open: tap card **art** (field/hand), or tap any card in builder/log/rules. Close: ✕ / tap-outside / back-gesture. It's a pure overlay — opening it changes no game state.

**Desktop (right-docked panel or centered modal) & mobile (full-screen sheet):**

```
┌──────────────── CARD ────────────────┐
│                          [ ‹ ]  [ › ] │
│   ┌───────────┐   Stardust Dragon     │
│   │  [ art ]  │   Synchro · WIND · Lv8│
│   │           │   ATK 2500 / DEF 2000 │
│   └───────────┘                       │
│                                       │
│   "1 Tuner + 1 or more non-Tuner…     │
│    During either player's turn, when   │
│    a card or effect is activated that  │
│    would destroy a card(s)… you can    │
│    Tribute this card to negate…"       │
│                                       │
│   Rulings (Edison): …                  │  ← static reference text only
│                             [ ✕ Close]│
└───────────────────────────────────────┘
```

**States.** Full card (owned/visible) · face-down opponent card = **not inspectable** (shows only "Set card" — never leaks hidden info) · from-builder (adds "Add to deck" / copy-count controls) · from-field (read-only). Rulings block hidden if we have no verified content for that card (never invent — accuracy is sacred).

**Notes.** The Inspector is the payoff of the inspect-vs-act separation (§15). One deliberate gesture, always safe.

---

## §10. Deck Builder — Desktop

**Purpose.** Meet the best incumbent builders (Dueling Nexus, edisonformat.net) on filtering/preview, then beat them on **live legality visualization** and **being genuinely usable on mobile** (§11 note). You should be unable to *accidentally* build an illegal deck, and you should absorb the banlist just by seeing it.

**Key elements.**
- **Search bar** + **Filters** (the proven Nexus set): card pool (Edison on by default, toggleable), card type (Monster/Spell/Trap), monster Type, Attribute, Level, ATK/DEF range, effect-text search, banlist status.
- **Results grid** — each card carries a **persistent banlist badge**: 🚫 Forbidden · ① Limited (max 1) · ② Semi-Limited (max 2) · clean = 3-copy legal. Out-of-pool cards filtered out by default (visible "Edison pool" toggle so exclusion is a *choice*, not a mystery).
- **Deck bins:** **Main (40–60) · Extra (≤15) · Side (≤15)** with live counts + a validity chip. Edison Extra = **Fusion + Synchro only** — reflect exactly that; show no Xyz/Link categories.
- **Deck stats:** monster/spell/trap split, Level/Attribute curve, **Tuner count** (Synchro-relevant), copies-at-limit list.
- Card tap → **Inspector** (§9) with add/remove.

**Primary interactions.** Search/filter → click a result to add (respecting copy limits) → it lands in the correct bin. Adding a copy beyond a card's limit is **blocked** — the extra copy greys out and the badge shows the cap (this is a legality *indicator* + enforcement; it does not narrate *why* in prose). Remove from a bin by clicking it. Save → My Decks. `.ydk` import/export.

```
┌──────────────────────────── DECK BUILDER ─────────────────────────────────┐
│ Search [ blackwing          ]   Filters ▾            Deck: "Blackwings" 💾  │
├───────────────────┬────────────────────────────────┬──────────────────────┤
│ FILTERS           │ RESULTS                         │ DECK                 │
│ Pool: Edison ✓    │ [c ] [c ] [c①] [c ]             │ Main    39 / 40–60 ▓ │
│ Type: Monster ▾   │ [c🚫][c ] [c ] [c②]             │ Extra   12 / ≤15   ✓ │
│ Attr: DARK ▾      │ [c ] [c ] [c ] [c ]             │ Side     8 / ≤15   ✓ │
│ Level: any ▾      │  ↑🚫 Forbidden  ↑① Limited      │ ──────────────────── │
│ ATK/DEF: ___–___  │                                 │ Monsters 17 S 14 T 8 │
│ Effect: [____]    │ (click a card → inspect/add)    │ Tuners 6             │
│ Banlist: any ▾    │                                 │ Curve ▁▂▅▃▂          │
│                   │                                 │ At limit: Gale ①     │
│                   │                                 │ [Import .ydk][Export]│
└───────────────────┴────────────────────────────────┴──────────────────────┘
   Deck status:  ⚠  39 in Main (need ≥40)   —  otherwise Edison-legal
```

**States.** Empty (new deck) · editing · **invalid** (bin under/over count, or a Forbidden card present → validity chip red, deck can't be used in a challenge) · valid (green chip). Copy-at-limit (extra copies greyed with the cap badge).

**Notes.** Legality is *visualized as you build*, not validated only at the end — same teach-by-enforcement philosophy as the field, minus any prose explanation. The validity chip states the *count problem* factually ("39 in Main, need ≥40") — that's a bin-count status, not a per-card "why illegal" sentence, so it stays inside the V1 boundary.

---

## §11. Deck Builder — Mobile (portrait)

**Purpose.** This is exactly where edisonformat.net *gives up* ("not available on mobile"). We won't. One responsive builder, equally good on a phone — a concrete, checkable win over the Edison-native incumbent.

**Key elements / layout.** Three collapsible regions so a phone can do the desktop job by peeking one at a time:
- **Top:** search + a **Filters** button that opens a full-screen filter sheet.
- **Middle:** results as **3-across tappable tiles**, each with its banlist badge.
- **Bottom sheet:** the **deck** — a peek bar (Main/Extra/Side counts + validity chip) that expands to the full deck list.

**Primary interactions.** Tap a result → Inspector with **[Add]** (or a quick long-press to add without opening). Tap the bottom sheet to expand the deck; tap a deck card to remove. Copy-limit blocking identical to desktop.

```
┌───────────────────────┐        (deck sheet expanded)
│ 🔍[ blackwing   ] ⚙︎   │        ┌───────────────────────┐
│  Pool: Edison ✓        │        │ ▾ DECK  Main 39 ⚠      │
├───────────────────────┤        │        Extra 12 ✓      │
│ [c ] [c①] [c ]         │        │        Side  8 ✓       │
│ [c🚫][c ] [c ]         │        ├───────────────────────┤
│ [c ] [c ] [c②]         │        │ MAIN                  │
│ [c ] [c ] [c ]         │        │ 3× Blackwing – Bora   │
│                       │        │ 1× Gale ①  (at limit) │
├───────────────────────┤        │ 2× Kalut …            │
│ ▴ DECK  Main 39 ⚠      │◄ peek  │ EXTRA (Fusion/Synchro)│
│         (tap to open)  │        │ 1× Stardust Dragon …  │
└───────────────────────┘        │ [Import][Export][Save]│
                                  └───────────────────────┘
```

**States.** Same as desktop (§10). Filter sheet: open/closed. Deck sheet: peek/expanded.

**Notes.** Everything is tap-based; no drag required (drag fights scroll on touch). The Inspector (§9) is the shared preview, so a card looks and reads identically here, on the field, and in the rules page.

---

## §12. My Decks

**Purpose.** Manage the small set of decks a player actually brings to duels. List, open in builder, duplicate, import/export, delete — the "which deck do I bring?" surface, feeding both the Challenge flow (§4) and Pre-Duel Room (§5).

**Key elements.** Deck cards showing name, a representative art/thumbnail, Main/Extra/Side counts, and a **legality chip** (✓ Edison-legal / ⚠ issues). Per-deck actions: **Open · Duplicate · Export .ydk · Delete**. **[+ New deck]** and **[Import .ydk]**.

**Primary interactions.** Tap a deck → open in Builder. New/Import → Builder. Duplicate → editable copy. Illegal decks are flagged and cannot be selected in a challenge until fixed.

**Desktop (grid) / Mobile (single-column list):**

```
┌──────────────── MY DECKS ────────────────┐     ┌───────────────────────┐
│ [+ New deck]        [ Import .ydk ]        │     │ MY DECKS      [+][⇩]  │
│                                           │     ├───────────────────────┤
│ ┌────────────┐ ┌────────────┐ ┌─────────┐ │     │ Blackwings   ✓        │
│ │ Blackwings │ │ Quickdraw  │ │ Test brew│ │     │ 40/13/9   [⋯]         │
│ │ M40 E13 S9 │ │ M40 E15 S6 │ │ M39 E10 │ │     ├───────────────────────┤
│ │  ✓ legal   │ │  ✓ legal   │ │ ⚠ 2 issu│ │     │ Quickdraw    ✓        │
│ │ [Open][⋯]  │ │ [Open][⋯]  │ │[Open][⋯]│ │     │ 40/15/6   [⋯]         │
│ └────────────┘ └────────────┘ └─────────┘ │     ├───────────────────────┤
│  ⋯ = Duplicate · Export · Delete           │     │ Test brew    ⚠ 2      │
└─────────────────────────────────────────────┘     │ 39/10/0   [⋯]         │
                                                    └───────────────────────┘
```

**States.** Has-decks · empty ("Build your first deck" → Builder) · deck-invalid flag · export/import in progress.

**Notes.** Import/export uses the community-standard `.ydk` format so decks move between this app, edisonformat.net, and their in-person prep — an explicit interop win for the group.

---

## §13. Duel Summary / Log Replay

**Purpose.** The post-duel recap and the reviewable action log. Even without the V2 "why" layer, this is the teaching artifact: you can *see exactly what happened, in order* — the neutral event history — and step through it. (No plain-language why-explanations; captions state events, not judgments.)

**Key elements.** Result banner (Win/Loss + condition: LP to 0 / deck-out / surrender / disconnect); **LP-over-time graph** for both players; **turn-by-turn action log** (expandable per turn; each entry taps into the Inspector for the card involved); **[Rematch]** and **[Done]**; **[▶ Replay]** — a scrubbable playback that re-runs the recorded duel-message stream on the real board with the same chain/resolution visualization (§7).

**Primary interactions.** Scrub/step the replay (⏮ ◀ ▶ ⏭, jump to a turn from the log); tap any log entry → Inspector or "jump replay here." Rematch → Pre-Duel Room with decks pre-filled. Done → Home.

```
┌──────────────── DUEL SUMMARY ────────────────┐
│   🏆  You win —  Alex's LP reached 0           │
│                                               │
│   LP  8000┤██                                 │
│           │  ██▄        ▄▄▄▄  you              │
│           │     ▀▀▄▄  ▄▀    ▀▀                 │
│      Alex │         ▀▀            → 0          │
│           └───────────────── turns ─────────  │
│                                               │
│   ── ACTION LOG ─────────────────────────────  │
│   ▸ Turn 1 · Alex — draw, set 1, pass         │
│   ▾ Turn 2 · You — Summon Bora, attack (1700) │
│       • Chain: Bottomless (Alex) → resolved   │
│   ▸ Turn 3 · Alex — …                          │
│                                               │
│   [ ▶ Replay ]        [ Rematch ]   [ Done ]   │
└─────────────────────────────────────────────────┘
```

**Mobile (portrait):** result banner + LP graph on top, action log as the scrollable body, **Replay / Rematch / Done** pinned bottom; replay opens the mobile field (§15) in a scrub mode.

**States.** Win · loss · draw · abandoned/DC (states the condition factually) · replay-playing/paused. Replay honors reduced-motion (snap transitions).

**Notes.** The log's neutrality is deliberate and consistent with the no-why decision: it's a faithful event record, the same data the V2 chatbot will later narrate. Replays persist at least for the most recent duel (surfaced on Home §3); retention depth is an eng/storage call.

---

## §14. Settings / Profile

**Purpose.** Identity + comfort. Small, because a six-person club needs a display name, a theme, and the accessibility controls that make dense card text usable for everyone.

**Key elements.**
- **Profile:** display name, avatar/card-back choice, sign out.
- **Appearance:** **Dark mode** (light / dark / system) — the community reaches for dark-mode extensions on incumbents; ship it natively.
- **Accessibility (first-class, not buried):** **Text size** (S / M / L / XL, honoring OS setting), **Reduced motion** (keeps LP/chain feedback informative without animation), **Colorblind-safe mode** (reinforces every state's icon/shape/label so nothing rides on hue alone).
- **Duel prefs:** confirm-before-end-turn toggle; (reserved, off in V1) response timer.

**Primary interactions.** Toggle → applies immediately and persists per user. Reachable from Home and from a slide-in inside the Duel Field so a player can bump text size or dark mode *mid-duel* without leaving.

```
┌──────────────── SETTINGS ────────────────┐
│ PROFILE                                   │
│  Display name [ you________ ]             │
│  Card back    [ ⬛ ▾ ]     [ Sign out ]   │
│                                           │
│ APPEARANCE                                │
│  Theme        (•) System ( ) Light ( ) Dk │
│                                           │
│ ACCESSIBILITY                             │
│  Text size    [ S ][ M ][•L ][ XL ]       │
│  Reduced motion            [ ●— on  ]     │
│  Colorblind-safe mode      [ —○ off ]     │
│                                           │
│ DUEL                                      │
│  Confirm before End Turn   [ ●— on  ]     │
└─────────────────────────────────────────────┘
```

**States.** Per-toggle on/off; changes apply live. Mobile = same, single column full-screen.

**Notes.** Accessibility settings are treated as core, not an afterthought, because **card text is the game** (§16). These toggles feed the global conventions in §0.

---

## §15. The Mobile 2-player Board — the central V1 UX risk (carried forward + refined)

A 2-player, ~22–24-zone board on a ~375-px phone is the single biggest UX risk in V1. Cramming both full fields at readable size is impossible — so we **don't**. Refined strategy:

**(a) Portrait "your-field-first" vertical stack — you never zoom to act.**
Your half gets the bottom ~55–60% at full size. The opponent's half is compressed into a **status strip** up top (their LP, hand count, and a *thumbnail* zone row) that **expands to full detail on tap (▾)**. Rationale: ~90% of the time you act on *your* board and only *read* the opponent's. Optimize the common case; make the rare case one tap away.

```
┌───────────────────────┐        (opp expanded via ▾)
│ Alex  LP 5400  ✋4  ▾  │        ┌───────────────────────┐
│ [m][m][m][m][m] mini   │        │ ▴ Alex  LP 5400  ✋4    │
│ [s][s][s][s][s] mini   │        │ [GY][Bn] [M][M][M][M][M]│
├───────────────────────┤        │ [Fld]    [S][S][S][S][S]│  full opp board
│  ⚔  YOUR MAIN 1        │        │           [Deck][Extra] │  (tap ▴ to collapse)
├───────────────────────┤        └───────────────────────┘
│ [S/T][S/T][S/T][S/T][S]│  ← YOUR board, full size, ≥44px targets
│ [ M ][ M ][ M ][ M ][M]│
│ GY  Ban   Field  Deck  │
├───────────────────────┤
│  YOUR HAND  (swipe →)  │  ← horizontally scrollable fan
│  [card][card][card•]   │
├───────────────────────┤
│ ◀ DP SP [MP1] BP MP2 EP│  ← compact phase rail
│           [ End Turn ▸]│
└───────────────────────┘
```

**(b) Tap-to-select, never drag-required.** Drag is fragile on touch (scroll conflicts, fat-finger drops) and impossible one-handed. Model: **tap a card → a bottom action-sheet of its legal actions → tap the action → tap the destination (which pulses).** Every action is 2–3 forgiving, cancelable taps. In V1 the sheet lists **only currently-legal actions** (no "why not" line — that's V2). Nothing commits until the final confirm/destination tap.

```
   Tap a hand card →              (V1: legal actions only; no "why not?")
   ┌───────────────────┐
   │  Stardust Dragon  │
   │ ▸ Special Summon  │
   │ ▸ Set             │
   │ ✕ Cancel          │
   └───────────────────┘
```

**(c) Inspect/zoom is a separate, deliberate gesture.** Tap the card's **art** (not its action region) or **long-press** → full-screen Card Inspector (§9). Separating "inspect" from "act" kills the Duelingbook problem where every touch risks doing something. **Outcome:** you can always read a card without fear of triggering it.

**(d) Response prompts (§7) are bottom sheets** so **[Respond]/[Pass]** sit under the thumb; the chain stack is a peekable panel above them. The opponent's status strip stays visible so you can see what you're responding to.

**(e) Landscape offered, not required.** Landscape can show a scaled-down both-fields "tabletop" view for players who want it, but portrait one-field-first is primary — phones are held portrait and Edison duels run long.

**(f) Responsive continuity.** Phone-portrait (this) → tablet (both fields, condensed) → desktop (§6 full) is one component system reflowed, so the interaction grammar (tap → action sheet → pulsing destination; tap-art-to-inspect) is *identical* everywhere. One mental model; a couch-phone friend and a desk friend are equally competent.

---

## §16. Accessibility & readability requirements (non-negotiable — card text *is* the game)

- **Two-tier card rendering.** At board/zone size: art + name + key stats (ATK/DEF/Level/Attribute) legible; **never** require reading full effect text at zone size. Full text lives one deliberate tap away in the Inspector (§9).
- **Tap targets ≥ 44 px; body/effect text ≥ 16 px on mobile.** Type scale honors the OS/browser text-size setting and the in-app Text-size control (§14).
- **No meaning by color alone.** Banlist status (🚫/①/②), ownership (you vs. opponent), and card states (actionable/dimmed/selected/valid-target) each pair color with an icon, shape, or label. Colorblind-safe mode strengthens these cues. (~8% of men have a color-vision deficiency.)
- **Reduced-motion mode** keeps LP-swing, chain, and resolution feedback fully informative without animation — for vestibular sensitivity and low-end phones. Replays and LP deltas snap instead of animate.
- **Contrast + dark mode** shipped natively (light/dark/system), meeting WCAG AA contrast for text and essential UI.
- **The board answers the three questions at a glance** (whose turn/phase, the score, what it's waiting on you for) at every text size and in both themes — accessibility can't break the core legibility contract.
- **Focus/keyboard on desktop** (nice-to-have but low-cost): phase rail, End Turn, and prompt buttons are keyboard-reachable; the enforcing model means keyboard users can't make illegal moves either.

---

## §17. Handoff notes / open questions for the CTO & engineering

1. **Legal-action surfacing is the contract this UI depends on.** Every "actionable marker," action sheet, targeting pulse, and response prompt assumes the engine can tell the client *the set of currently-legal decisions/actions for the active player* (which ocgcore does via decision-request messages). V1 needs **which actions are legal**, not *why others aren't* — so the removed V2 reason-mapping layer is genuinely out of scope and does not block V1.
2. **Edison ruleset must be configured for the 2010 environment**, and the response-window UI (§7) depends on it: **Ignition Effect Priority is IN EFFECT** (turn player may activate an Ignition Effect with priority after a Summon before passing SS2 window); **player going first draws on turn 1** (pre-MR3); one Field Spell zone behavior of the era; Damage Step timing pre-MR3. Confirm the engine's ruleset flags express MR1-era priority, or flag the gap early — this shapes what prompts the field must render.
3. **Hidden information never reaches the client.** Opponent hand = count only; face-down cards = card-back + "Set card," non-inspectable (§6/§9). Assumes server-authoritative state (per engine research) — if a WASM/local path is used for goldfish/replay, it must not expose hidden zones in live 1v1.
4. **Deck legality data.** The builder badges (§10/§11) and challenge-time blocking (§4) need the frozen Edison passcode allow-list + `lflist.conf` (from the edison-pool-lock decision). Copy limits (F0/L1/S2) and bin limits (Main 40–60, Extra ≤15, Side ≤15) are enforced client-side for UX responsiveness **and** server-side for authority.
5. **Card images at multiple sizes.** Field/hand/thumbnail/inspector all need crisp, text-legible art on hi-dpi phones. Source/hosting is the CTO's call; the design assumes it's available and legible (task premise).
6. **`.ydk` import/export** (§11/§12) for interop with the group's existing tools — confirm format parity.
7. **Replay storage.** §13 assumes at least the most-recent duel's message stream is persisted; retention depth is a storage decision.
8. **The V2 seam exists but ships nothing.** §7 prompts, §9 Inspector rulings block, and §13 log are laid out so a later "why did/couldn't that happen?" affordance drops in without relayout. Do **not** build the reason-mapping layer, the disabled-action tooltips, or the disabled-action → rules deep-links in V1.

---

## §18. Explicitly NOT in V1 (so nothing creeps back in)

- ❌ "Why is this greyed out?" plain-language reason tooltips on disabled cards/actions.
- ❌ Deep-links from disabled actions into the Rules & Rulings page.
- ❌ The `{engine reason → human string → rules anchor}` reason-mapping layer.
- ❌ The in-duel "why did that happen?" chatbot (V2).
- ❌ Ranked/matchmaking, AI opponent, spectating, tournaments, cosmetics/gacha, native app — none serve six friends dueling each other.

Disabled/illegal actions may still be shown **dimmed as a state**; they simply carry no tappable text explanation in V1. The Rules & Rulings page (§ below) still exists as a standalone reference — it just isn't wired to disabled-action deep-links in V1.

---

## Rules & Rulings Reference (standalone — included for completeness; unchanged in intent, minus V1 deep-links)

**Purpose.** A searchable, sectioned static reference covering the game **through Edison** — turn structure/phases, summon types (incl. Synchro/Tuners, Fusion, Ritual), chains & priority/SEGOC, card types, and Edison-specific banlist + notable rulings. Readable entirely on its own (a friend can bookmark it and learn without opening a duel).

**Key elements.** Sticky search; collapsible sections; large type; deep-linkable anchors (the anchors exist for future use, but in V1 nothing in the duel auto-links into them — that wiring is V2). Reachable from Home (§3) and a slide-in inside the Duel Field.

**Desktop (two-pane) / Mobile (accordion):**

```
┌──────────────── RULES & RULINGS ────────────────┐    ┌───────────────────────┐
│ 🔍[ priority                    ]                │    │ 🔍[ chains        ]    │
├───────────────┬─────────────────────────────────┤    ├───────────────────────┤
│ CONTENTS      │  Priority (Edison / MR1 era)     │    │ ▸ Turn structure       │
│ ▸ Turn phases │                                  │    │ ▸ Summoning            │
│ ▸ Summoning   │  In this format, the turn player │    │ ▾ Chains & priority    │
│ ▾ Chains &    │  may activate an Ignition Effect │    │    • Spell Speed       │
│    priority   │  with priority after Summoning…  │    │    • SEGOC             │
│ ▸ Card types  │                                  │    │    • Ignition priority │
│ ▸ Banlist     │  [ related cards → Inspector ]   │    │ ▸ Card types           │
└───────────────┴─────────────────────────────────┘    │ ▸ Edison banlist       │
                                                        └───────────────────────┘
```

**States.** Browsing · search-results · section-expanded. Mobile: sticky search + accordion; same readability duty as card text (§16).

**Notes.** Content sourced from verified community-trusted material (accuracy is sacred; never invent rulings). It is the standalone teaching backbone; the *connective* teaching (in-duel why, chatbot) is deliberately V2.

---

*End of V1 flows & wireframes. Screens covered: Login · Home/Lobby · Challenge flow · Pre-Duel Room · Duel Field (desktop + mobile) · Priority/Chain/Resolution · Card Inspector · Deck Builder (desktop + mobile) · My Decks · Duel Summary/Replay · Settings/Profile · Rules & Rulings. Flow map in §1.*
